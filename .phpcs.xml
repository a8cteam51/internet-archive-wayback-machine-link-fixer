<?xml version="1.0"?>
<ruleset name="wayback_link_fixer" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="vendor/squizlabs/php_codesniffer/phpcs.xsd">
	<description>Custom ruleset for the Team51 plugin.</description>

    <exclude-pattern>/lib/*</exclude-pattern>
	<exclude-pattern>/vendor/*</exclude-pattern>
	<exclude-pattern>/wordpress/*</exclude-pattern>
	<exclude-pattern>/tests/*</exclude-pattern>
	<exclude-pattern>/phpunit.xml.dist</exclude-pattern>
	<exclude-pattern>/coverage/*</exclude-pattern>
	<exclude-pattern>/coverage-report/*</exclude-pattern>

	<!-- Extend the A8CTeam51 ruleset. -->
	<rule ref="./vendor/a8cteam51/team51-configs/quality-tools/phpcs.xml.dist"/>

	<!-- Check that the proper text domain(s) is used everywhere. -->
	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array">
				<element value="internet-archive-wayback-machine-link-fixer"/>
			</property>
		</properties>
	</rule>

	<!-- Check that the proper prefix is used everywhere. -->
	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="wlf_"/>
				<element value="wpcomsp_"/>
				<element value="wpcomsp_wayback_link_fixer_"/>
				<element value="WPCOMSpecialProjects\Wayback_Link_Fixer"/>
			</property>
		</properties>
	</rule>
</ruleset>
